package main

var (
	// IP ip address on which it will listen for client connections
	IP string
	// PORT the port number
	PORT string
	// DIRECTORY the server directory
	DIRECTORY string
	// TIMEOUT timeout for each worker in ms
	TIMEOUT int
)
